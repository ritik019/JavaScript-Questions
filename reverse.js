// for online compiler

let inputStr = prompt("Enter the input: ");
let reversedStr = reverseStr(inputStr);
console.log(reversedStr);

function reverseStr(str) {
    return str.split("").reverse().join("");
}


// if we give our input
console.log(reverseStr("Sakshyat"));
function reverseStr(str) {
    return str.split("").reverse().join("");
}
